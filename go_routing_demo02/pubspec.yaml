name: go_routing_demo01
description: A new Flutter project.

environment:
  sdk: '^3.11.0'

dependencies:
  flutter:
    sdk: flutter
  go_router: ^17.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.6.0
  go_router_builder: ^4.0.1
